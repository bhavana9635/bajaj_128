spring.main.banner-mode=off
logging.level.root=INFO
logging.level.org.springframework.web=INFO

# Required participant details
app.name=John Doe
app.regNo=REG12347
app.email=john@example.com

# Optional: Provide final SQL directly (overrides resource files if set)
# app.finalQuery=SELECT 1;

# Resource paths for SQL (used if app.finalQuery is not set)
app.question1SqlPath=sql/question1.sql
app.question2SqlPath=sql/question2.sql


